docker-machine create --driver=generic --generic-ip-address=... --generic-ssh-user=root --generic-ssh-key=/Users/simon/.ssh/... droplet-docker

docker-machine env droplet-docker
eval $(docker-machine env docker-mulser)

docker build -t slash-commands -f slash-commands/Dockerfile .
docker run -d slash-commands
sudo docker exec -it loving_heisenberg /bin/bash
